# Collections Framework - Part 6

## Learning Goal

This file covers a focused slice of **Collections Framework**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Fail-fast iterator` | An Iterator traverses a collection while hiding its internal representation. |
| `Fail-safe iterator` | An Iterator traverses a collection while hiding its internal representation. |
| `ConcurrentModificationException` | An exception represents an abnormal condition that a program may catch or propagate. |
| `Collections.sort` | A collection is an object that groups multiple elements under a common API. |
| `Collections.reverse` | A collection is an object that groups multiple elements under a common API. |
| `Collections.shuffle` | A collection is an object that groups multiple elements under a common API. |
| `Collections.max` | A collection is an object that groups multiple elements under a common API. |
| `Collections.min` | A collection is an object that groups multiple elements under a common API. |

## Detailed Notes

### Fail-fast iterator

An Iterator traverses a collection while hiding its internal representation.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `Fail-fast iterator` in one sentence.
- Recognize `Fail-fast iterator` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Fail-fast iterator`.

Tiny example or mental model:

- When reading code, ask: what does `Fail-fast iterator` change, allow, reject, or clarify?

### Fail-safe iterator

An Iterator traverses a collection while hiding its internal representation.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `Fail-safe iterator` in one sentence.
- Recognize `Fail-safe iterator` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Fail-safe iterator`.

Tiny example or mental model:

- When reading code, ask: what does `Fail-safe iterator` change, allow, reject, or clarify?

### ConcurrentModificationException

An exception represents an abnormal condition that a program may catch or propagate.

It matters because exception behavior decides whether failures are handled locally, propagated, or allowed to stop the program. A common confusion is treating every exception the same instead of separating recoverable conditions from programming bugs.

Practical check:

- Define `ConcurrentModificationException` in one sentence.
- Recognize `ConcurrentModificationException` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `ConcurrentModificationException`.

Tiny example or mental model:

- When reading code, ask: what does `ConcurrentModificationException` change, allow, reject, or clarify?

### Collections.sort

A collection is an object that groups multiple elements under a common API.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `Collections.sort` in one sentence.
- Recognize `Collections.sort` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Collections.sort`.

Tiny example or mental model:

- When reading code, ask: what does `Collections.sort` change, allow, reject, or clarify?

### Collections.reverse

A collection is an object that groups multiple elements under a common API.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `Collections.reverse` in one sentence.
- Recognize `Collections.reverse` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Collections.reverse`.

Tiny example or mental model:

- When reading code, ask: what does `Collections.reverse` change, allow, reject, or clarify?

### Collections.shuffle

A collection is an object that groups multiple elements under a common API.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `Collections.shuffle` in one sentence.
- Recognize `Collections.shuffle` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Collections.shuffle`.

Tiny example or mental model:

- When reading code, ask: what does `Collections.shuffle` change, allow, reject, or clarify?

### Collections.max

A collection is an object that groups multiple elements under a common API.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `Collections.max` in one sentence.
- Recognize `Collections.max` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Collections.max`.

Tiny example or mental model:

- When reading code, ask: what does `Collections.max` change, allow, reject, or clarify?

### Collections.min

A collection is an object that groups multiple elements under a common API.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `Collections.min` in one sentence.
- Recognize `Collections.min` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Collections.min`.

Tiny example or mental model:

- When reading code, ask: what does `Collections.min` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
