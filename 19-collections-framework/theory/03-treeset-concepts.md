# Collections Framework - Part 3

## Learning Goal

This file covers a focused slice of **Collections Framework**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `TreeSet` | A Set is a collection that rejects duplicates according to equality rules. |
| `SortedSet` | A Set is a collection that rejects duplicates according to equality rules. |
| `NavigableSet` | A Set is a collection that rejects duplicates according to equality rules. |
| `When to use Set?` | A Set is a collection that rejects duplicates according to equality rules. |
| `Duplicate removal mechanism` |Duplicate removal mechanism is a specific concept in Collections Framework; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Role of equals() and hashCode()` | equals() defines logical equality between objects. |
| `PriorityQueue` |PriorityQueue removes elements by priority rather than insertion order. |
| `ArrayDeque` |ArrayDeque is a resizable-array Deque often preferred for stack or queue behavior. |

## Detailed Notes

### TreeSet

A Set is a collection that rejects duplicates according to equality rules.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `TreeSet` in one sentence.
- Recognize `TreeSet` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `TreeSet`.

Tiny example or mental model:

- When reading code, ask: what does `TreeSet` change, allow, reject, or clarify?

### SortedSet

A Set is a collection that rejects duplicates according to equality rules.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `SortedSet` in one sentence.
- Recognize `SortedSet` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `SortedSet`.

Tiny example or mental model:

- When reading code, ask: what does `SortedSet` change, allow, reject, or clarify?

### NavigableSet

A Set is a collection that rejects duplicates according to equality rules.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `NavigableSet` in one sentence.
- Recognize `NavigableSet` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `NavigableSet`.

Tiny example or mental model:

- When reading code, ask: what does `NavigableSet` change, allow, reject, or clarify?

### When to use Set?

A Set is a collection that rejects duplicates according to equality rules.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `When to use Set?` in one sentence.
- Recognize `When to use Set?` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `When to use Set?`.

Tiny example or mental model:

- When reading code, ask: what does `When to use Set?` change, allow, reject, or clarify?

### Duplicate removal mechanism

Duplicate removal mechanism is a specific concept in Collections Framework; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Duplicate removal mechanism` in one sentence.
- Recognize `Duplicate removal mechanism` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Duplicate removal mechanism`.

Tiny example or mental model:

- When reading code, ask: what does `Duplicate removal mechanism` change, allow, reject, or clarify?

### Role of equals() and hashCode()

equals() defines logical equality between objects.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `Role of equals() and hashCode()` in one sentence.
- Recognize `Role of equals() and hashCode()` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Role of equals() and hashCode()`.

Tiny example or mental model:

- When reading code, ask: what does `Role of equals() and hashCode()` change, allow, reject, or clarify?

### PriorityQueue

PriorityQueue removes elements by priority rather than insertion order.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `PriorityQueue` in one sentence.
- Recognize `PriorityQueue` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `PriorityQueue`.

Tiny example or mental model:

- When reading code, ask: what does `PriorityQueue` change, allow, reject, or clarify?

### ArrayDeque

ArrayDeque is a resizable-array Deque often preferred for stack or queue behavior.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `ArrayDeque` in one sentence.
- Recognize `ArrayDeque` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `ArrayDeque`.

Tiny example or mental model:

- When reading code, ask: what does `ArrayDeque` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
