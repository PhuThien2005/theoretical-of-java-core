# Regular Expression - Part 1

## Learning Goal

This file covers a focused slice of **Regular Expression**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `What is Regex?` | A regular expression is a pattern language for matching text. |
| `Pattern` |Pattern is the compiled representation of a regular expression. |
| `Matcher` |Matcher applies a Pattern to input text and exposes match operations. |
| `matches` |matches checks whether the entire input satisfies the pattern. |
| `find` |find searches for the next subsequence that matches the pattern. |
| `group` |group returns text captured by the overall match or a capturing group. |
| `Character classes` |Character classes define sets of allowed characters such as digits, letters, or custom ranges. |
| `Quantifiers` |Quantifiers specify how many times the preceding token may repeat. |
| `Capturing group` |A capturing group stores a matched subexpression for later retrieval. |
| `Non-capturing group` |A non-capturing group groups pattern logic without storing a captured result. |

## Detailed Notes

### What is Regex?

A regular expression is a pattern language for matching text.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `What is Regex?` in one sentence.
- Recognize `What is Regex?` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `What is Regex?`.

Tiny example or mental model:

- When reading code, ask: what does `What is Regex?` change, allow, reject, or clarify?

### Pattern

Pattern is the compiled representation of a regular expression.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Pattern` in one sentence.
- Recognize `Pattern` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Pattern`.

Tiny example or mental model:

- When reading code, ask: what does `Pattern` change, allow, reject, or clarify?

### Matcher

Matcher applies a Pattern to input text and exposes match operations.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Matcher` in one sentence.
- Recognize `Matcher` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Matcher`.

Tiny example or mental model:

- When reading code, ask: what does `Matcher` change, allow, reject, or clarify?

### matches

matches checks whether the entire input satisfies the pattern.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `matches` in one sentence.
- Recognize `matches` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `matches`.

Tiny example or mental model:

- When reading code, ask: what does `matches` change, allow, reject, or clarify?

### find

find searches for the next subsequence that matches the pattern.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `find` in one sentence.
- Recognize `find` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `find`.

Tiny example or mental model:

- When reading code, ask: what does `find` change, allow, reject, or clarify?

### group

group returns text captured by the overall match or a capturing group.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `group` in one sentence.
- Recognize `group` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `group`.

Tiny example or mental model:

- When reading code, ask: what does `group` change, allow, reject, or clarify?

### Character classes

Character classes define sets of allowed characters such as digits, letters, or custom ranges.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Character classes` in one sentence.
- Recognize `Character classes` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Character classes`.

Tiny example or mental model:

- When reading code, ask: what does `Character classes` change, allow, reject, or clarify?

### Quantifiers

Quantifiers specify how many times the preceding token may repeat.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Quantifiers` in one sentence.
- Recognize `Quantifiers` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Quantifiers`.

Tiny example or mental model:

- When reading code, ask: what does `Quantifiers` change, allow, reject, or clarify?

### Capturing group

A capturing group stores a matched subexpression for later retrieval.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Capturing group` in one sentence.
- Recognize `Capturing group` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Capturing group`.

Tiny example or mental model:

- When reading code, ask: what does `Capturing group` change, allow, reject, or clarify?

### Non-capturing group

A non-capturing group groups pattern logic without storing a captured result.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Non-capturing group` in one sentence.
- Recognize `Non-capturing group` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Non-capturing group`.

Tiny example or mental model:

- When reading code, ask: what does `Non-capturing group` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
