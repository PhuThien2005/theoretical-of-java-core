# Regular Expression - Part 2

## Learning Goal

This file covers a focused slice of **Regular Expression**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Basic lookahead / lookbehind` |Basic lookahead / lookbehind is a specific concept in Regular Expression; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Validate email, phone, password` |Validate email, phone, password is a specific concept in Regular Expression; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Replace using regex` | A regular expression is a pattern language for matching text. |
| `Split using regex` | A regular expression is a pattern language for matching text. |

## Detailed Notes

### Basic lookahead / lookbehind

Basic lookahead / lookbehind is a specific concept in Regular Expression; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Basic lookahead / lookbehind` in one sentence.
- Recognize `Basic lookahead / lookbehind` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Basic lookahead / lookbehind`.

Tiny example or mental model:

- When reading code, ask: what does `Basic lookahead / lookbehind` change, allow, reject, or clarify?

### Validate email, phone, password

Validate email, phone, password is a specific concept in Regular Expression; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Validate email, phone, password` in one sentence.
- Recognize `Validate email, phone, password` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Validate email, phone, password`.

Tiny example or mental model:

- When reading code, ask: what does `Validate email, phone, password` change, allow, reject, or clarify?

### Replace using regex

A regular expression is a pattern language for matching text.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Replace using regex` in one sentence.
- Recognize `Replace using regex` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Replace using regex`.

Tiny example or mental model:

- When reading code, ask: what does `Replace using regex` change, allow, reject, or clarify?

### Split using regex

A regular expression is a pattern language for matching text.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Split using regex` in one sentence.
- Recognize `Split using regex` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Split using regex`.

Tiny example or mental model:

- When reading code, ask: what does `Split using regex` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
