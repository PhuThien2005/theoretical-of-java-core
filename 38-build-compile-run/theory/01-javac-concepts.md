# Build, Compile, Run - Part 1

## Learning Goal

This file covers a focused slice of **Build, Compile, Run**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `javac` |javac is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `java` |java is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `jar` |jar is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Create JAR file` |Create JAR file is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Executable JAR` |Executable JAR is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Classpath` | Classpath tells the JVM and compiler where to find classes and JARs. |
| `Manifest file` |Manifest file is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Basic Maven` | Maven is a build and dependency-management tool that uses conventions and a pom.xml file. |
| `Basic Gradle` | Gradle is a flexible build tool commonly used for JVM projects. |
| `Dependency management` |Dependency management is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### javac

javac is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `javac` in one sentence.
- Recognize `javac` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `javac`.

Tiny example or mental model:

- When reading code, ask: what does `javac` change, allow, reject, or clarify?

### java

java is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `java` in one sentence.
- Recognize `java` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `java`.

Tiny example or mental model:

- When reading code, ask: what does `java` change, allow, reject, or clarify?

### jar

jar is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `jar` in one sentence.
- Recognize `jar` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `jar`.

Tiny example or mental model:

- When reading code, ask: what does `jar` change, allow, reject, or clarify?

### Create JAR file

Create JAR file is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Create JAR file` in one sentence.
- Recognize `Create JAR file` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Create JAR file`.

Tiny example or mental model:

- When reading code, ask: what does `Create JAR file` change, allow, reject, or clarify?

### Executable JAR

Executable JAR is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Executable JAR` in one sentence.
- Recognize `Executable JAR` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Executable JAR`.

Tiny example or mental model:

- When reading code, ask: what does `Executable JAR` change, allow, reject, or clarify?

### Classpath

Classpath tells the JVM and compiler where to find classes and JARs.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Classpath` in one sentence.
- Recognize `Classpath` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Classpath`.

Tiny example or mental model:

- When reading code, ask: what does `Classpath` change, allow, reject, or clarify?

### Manifest file

Manifest file is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Manifest file` in one sentence.
- Recognize `Manifest file` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Manifest file`.

Tiny example or mental model:

- When reading code, ask: what does `Manifest file` change, allow, reject, or clarify?

### Basic Maven

Maven is a build and dependency-management tool that uses conventions and a pom.xml file.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Basic Maven` in one sentence.
- Recognize `Basic Maven` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Basic Maven`.

Tiny example or mental model:

- When reading code, ask: what does `Basic Maven` change, allow, reject, or clarify?

### Basic Gradle

Gradle is a flexible build tool commonly used for JVM projects.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Basic Gradle` in one sentence.
- Recognize `Basic Gradle` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Basic Gradle`.

Tiny example or mental model:

- When reading code, ask: what does `Basic Gradle` change, allow, reject, or clarify?

### Dependency management

Dependency management is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Dependency management` in one sentence.
- Recognize `Dependency management` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Dependency management`.

Tiny example or mental model:

- When reading code, ask: what does `Dependency management` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
