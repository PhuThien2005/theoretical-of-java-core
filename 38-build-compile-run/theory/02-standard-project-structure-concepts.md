# Build, Compile, Run - Part 2

## Learning Goal

This file covers a focused slice of **Build, Compile, Run**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Standard project structure` |Standard project structure is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Basic unit test with JUnit` | JUnit is a Java testing framework for writing and running automated tests. |

## Detailed Notes

### Standard project structure

Standard project structure is a specific concept in Build, Compile, Run; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Standard project structure` in one sentence.
- Recognize `Standard project structure` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Standard project structure`.

Tiny example or mental model:

- When reading code, ask: what does `Standard project structure` change, allow, reject, or clarify?

### Basic unit test with JUnit

JUnit is a Java testing framework for writing and running automated tests.

It matters because tests protect behavior during change. A common confusion is testing implementation details instead of observable behavior.

Practical check:

- Define `Basic unit test with JUnit` in one sentence.
- Recognize `Basic unit test with JUnit` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Basic unit test with JUnit`.

Tiny example or mental model:

- When reading code, ask: what does `Basic unit test with JUnit` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
