# IO in Java - Part 2

## Learning Goal

This file covers a focused slice of **IO in Java**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `BufferedInputStream` | A Stream is a pipeline for processing elements through lazy operations. |
| `BufferedOutputStream` | A Stream is a pipeline for processing elements through lazy operations. |
| `Reader` |Reader is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Writer` |Writer is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `FileReader` |FileReader is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `FileWriter` |FileWriter is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `BufferedReader` |BufferedReader is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `BufferedWriter` |BufferedWriter is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `ObjectInputStream` | A Stream is a pipeline for processing elements through lazy operations. |
| `ObjectOutputStream` | A Stream is a pipeline for processing elements through lazy operations. |

## Detailed Notes

### BufferedInputStream

A Stream is a pipeline for processing elements through lazy operations.

It matters because modern Java APIs use function-style pipelines heavily. A common confusion is forgetting which operations are lazy and which operation actually triggers execution.

Practical check:

- Define `BufferedInputStream` in one sentence.
- Recognize `BufferedInputStream` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `BufferedInputStream`.

Tiny example or mental model:

- When reading code, ask: what does `BufferedInputStream` change, allow, reject, or clarify?

### BufferedOutputStream

A Stream is a pipeline for processing elements through lazy operations.

It matters because modern Java APIs use function-style pipelines heavily. A common confusion is forgetting which operations are lazy and which operation actually triggers execution.

Practical check:

- Define `BufferedOutputStream` in one sentence.
- Recognize `BufferedOutputStream` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `BufferedOutputStream`.

Tiny example or mental model:

- When reading code, ask: what does `BufferedOutputStream` change, allow, reject, or clarify?

### Reader

Reader is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Reader` in one sentence.
- Recognize `Reader` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Reader`.

Tiny example or mental model:

- When reading code, ask: what does `Reader` change, allow, reject, or clarify?

### Writer

Writer is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Writer` in one sentence.
- Recognize `Writer` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Writer`.

Tiny example or mental model:

- When reading code, ask: what does `Writer` change, allow, reject, or clarify?

### FileReader

FileReader is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `FileReader` in one sentence.
- Recognize `FileReader` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `FileReader`.

Tiny example or mental model:

- When reading code, ask: what does `FileReader` change, allow, reject, or clarify?

### FileWriter

FileWriter is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `FileWriter` in one sentence.
- Recognize `FileWriter` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `FileWriter`.

Tiny example or mental model:

- When reading code, ask: what does `FileWriter` change, allow, reject, or clarify?

### BufferedReader

BufferedReader is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `BufferedReader` in one sentence.
- Recognize `BufferedReader` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `BufferedReader`.

Tiny example or mental model:

- When reading code, ask: what does `BufferedReader` change, allow, reject, or clarify?

### BufferedWriter

BufferedWriter is a specific concept in IO in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `BufferedWriter` in one sentence.
- Recognize `BufferedWriter` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `BufferedWriter`.

Tiny example or mental model:

- When reading code, ask: what does `BufferedWriter` change, allow, reject, or clarify?

### ObjectInputStream

A Stream is a pipeline for processing elements through lazy operations.

It matters because modern Java APIs use function-style pipelines heavily. A common confusion is forgetting which operations are lazy and which operation actually triggers execution.

Practical check:

- Define `ObjectInputStream` in one sentence.
- Recognize `ObjectInputStream` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `ObjectInputStream`.

Tiny example or mental model:

- When reading code, ask: what does `ObjectInputStream` change, allow, reject, or clarify?

### ObjectOutputStream

A Stream is a pipeline for processing elements through lazy operations.

It matters because modern Java APIs use function-style pipelines heavily. A common confusion is forgetting which operations are lazy and which operation actually triggers execution.

Practical check:

- Define `ObjectOutputStream` in one sentence.
- Recognize `ObjectOutputStream` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `ObjectOutputStream`.

Tiny example or mental model:

- When reading code, ask: what does `ObjectOutputStream` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
