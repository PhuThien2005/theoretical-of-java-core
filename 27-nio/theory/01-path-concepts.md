# NIO / NIO.2 - Part 1

## Learning Goal

This file covers a focused slice of **NIO / NIO.2**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Path` |Path is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Paths` |Paths is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Files` |Files is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `StandardOpenOption` |StandardOpenOption is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Read/write file using Files` |Read/write file using Files is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Walk file tree` |Walk file tree is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Copy/move/delete file` |Copy/move/delete file is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Channel` |Channel is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Buffer` |Buffer is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `ByteBuffer` |ByteBuffer is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### Path

Path is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Path` in one sentence.
- Recognize `Path` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Path`.

Tiny example or mental model:

- When reading code, ask: what does `Path` change, allow, reject, or clarify?

### Paths

Paths is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Paths` in one sentence.
- Recognize `Paths` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Paths`.

Tiny example or mental model:

- When reading code, ask: what does `Paths` change, allow, reject, or clarify?

### Files

Files is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Files` in one sentence.
- Recognize `Files` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Files`.

Tiny example or mental model:

- When reading code, ask: what does `Files` change, allow, reject, or clarify?

### StandardOpenOption

StandardOpenOption is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `StandardOpenOption` in one sentence.
- Recognize `StandardOpenOption` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `StandardOpenOption`.

Tiny example or mental model:

- When reading code, ask: what does `StandardOpenOption` change, allow, reject, or clarify?

### Read/write file using Files

Read/write file using Files is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Read/write file using Files` in one sentence.
- Recognize `Read/write file using Files` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Read/write file using Files`.

Tiny example or mental model:

- When reading code, ask: what does `Read/write file using Files` change, allow, reject, or clarify?

### Walk file tree

Walk file tree is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Walk file tree` in one sentence.
- Recognize `Walk file tree` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Walk file tree`.

Tiny example or mental model:

- When reading code, ask: what does `Walk file tree` change, allow, reject, or clarify?

### Copy/move/delete file

Copy/move/delete file is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Copy/move/delete file` in one sentence.
- Recognize `Copy/move/delete file` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Copy/move/delete file`.

Tiny example or mental model:

- When reading code, ask: what does `Copy/move/delete file` change, allow, reject, or clarify?

### Channel

Channel is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Channel` in one sentence.
- Recognize `Channel` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Channel`.

Tiny example or mental model:

- When reading code, ask: what does `Channel` change, allow, reject, or clarify?

### Buffer

Buffer is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Buffer` in one sentence.
- Recognize `Buffer` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Buffer`.

Tiny example or mental model:

- When reading code, ask: what does `Buffer` change, allow, reject, or clarify?

### ByteBuffer

ByteBuffer is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `ByteBuffer` in one sentence.
- Recognize `ByteBuffer` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `ByteBuffer`.

Tiny example or mental model:

- When reading code, ask: what does `ByteBuffer` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
