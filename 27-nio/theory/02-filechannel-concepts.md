# NIO / NIO.2 - Part 2

## Learning Goal

This file covers a focused slice of **NIO / NIO.2**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `FileChannel` |FileChannel is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Basic Selector` |Basic Selector is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Basic Asynchronous IO` |Basic Asynchronous IO is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### FileChannel

FileChannel is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `FileChannel` in one sentence.
- Recognize `FileChannel` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `FileChannel`.

Tiny example or mental model:

- When reading code, ask: what does `FileChannel` change, allow, reject, or clarify?

### Basic Selector

Basic Selector is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Basic Selector` in one sentence.
- Recognize `Basic Selector` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Basic Selector`.

Tiny example or mental model:

- When reading code, ask: what does `Basic Selector` change, allow, reject, or clarify?

### Basic Asynchronous IO

Basic Asynchronous IO is a specific concept in NIO / NIO.2; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Basic Asynchronous IO` in one sentence.
- Recognize `Basic Asynchronous IO` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Basic Asynchronous IO`.

Tiny example or mental model:

- When reading code, ask: what does `Basic Asynchronous IO` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
