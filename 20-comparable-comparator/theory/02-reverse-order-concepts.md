# Comparable and Comparator - Part 2

## Learning Goal

This file covers a focused slice of **Comparable and Comparator**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Reverse order` |Reverse order is a specific concept in Comparable and Comparator; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Null handling:` | Null handling is a group of related rules in Comparable and Comparator that groups several related details. |
| `nullsFirst` |nullsFirst is a specific concept in Comparable and Comparator; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `nullsLast` |nullsLast is a specific concept in Comparable and Comparator; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### Reverse order

Reverse order is a specific concept in Comparable and Comparator; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Reverse order` in one sentence.
- Recognize `Reverse order` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Reverse order`.

Tiny example or mental model:

- When reading code, ask: what does `Reverse order` change, allow, reject, or clarify?

### Null handling:

Null handling is a group of related rules in Comparable and Comparator that groups several related details.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Null handling:` in one sentence.
- Recognize `Null handling:` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Null handling:`.

Tiny example or mental model:

- When reading code, ask: what does `Null handling:` change, allow, reject, or clarify?

### nullsFirst

nullsFirst is a specific concept in Comparable and Comparator; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `nullsFirst` in one sentence.
- Recognize `nullsFirst` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `nullsFirst`.

Tiny example or mental model:

- When reading code, ask: what does `nullsFirst` change, allow, reject, or clarify?

### nullsLast

nullsLast is a specific concept in Comparable and Comparator; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `nullsLast` in one sentence.
- Recognize `nullsLast` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `nullsLast`.

Tiny example or mental model:

- When reading code, ask: what does `nullsLast` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
