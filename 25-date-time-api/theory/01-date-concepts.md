# Date and Time API - Part 1

## Learning Goal

This file covers a focused slice of **Date and Time API**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Date` |Date is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Calendar` |Calendar is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `SimpleDateFormat` |SimpleDateFormat is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `LocalDate` | LocalDate represents a date without time or time zone. |
| `LocalTime` |LocalTime is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `LocalDateTime` | LocalDate represents a date without time or time zone. |
| `ZonedDateTime` |ZonedDateTime is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `OffsetDateTime` | A Set is a collection that rejects duplicates according to equality rules. |
| `Instant` | Instant represents a point on the UTC timeline. |
| `Duration` |Duration is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### Date

Date is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Date` in one sentence.
- Recognize `Date` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Date`.

Tiny example or mental model:

- When reading code, ask: what does `Date` change, allow, reject, or clarify?

### Calendar

Calendar is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Calendar` in one sentence.
- Recognize `Calendar` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Calendar`.

Tiny example or mental model:

- When reading code, ask: what does `Calendar` change, allow, reject, or clarify?

### SimpleDateFormat

SimpleDateFormat is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `SimpleDateFormat` in one sentence.
- Recognize `SimpleDateFormat` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `SimpleDateFormat`.

Tiny example or mental model:

- When reading code, ask: what does `SimpleDateFormat` change, allow, reject, or clarify?

### LocalDate

LocalDate represents a date without time or time zone.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `LocalDate` in one sentence.
- Recognize `LocalDate` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `LocalDate`.

Tiny example or mental model:

- When reading code, ask: what does `LocalDate` change, allow, reject, or clarify?

### LocalTime

LocalTime is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `LocalTime` in one sentence.
- Recognize `LocalTime` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `LocalTime`.

Tiny example or mental model:

- When reading code, ask: what does `LocalTime` change, allow, reject, or clarify?

### LocalDateTime

LocalDate represents a date without time or time zone.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `LocalDateTime` in one sentence.
- Recognize `LocalDateTime` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `LocalDateTime`.

Tiny example or mental model:

- When reading code, ask: what does `LocalDateTime` change, allow, reject, or clarify?

### ZonedDateTime

ZonedDateTime is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `ZonedDateTime` in one sentence.
- Recognize `ZonedDateTime` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `ZonedDateTime`.

Tiny example or mental model:

- When reading code, ask: what does `ZonedDateTime` change, allow, reject, or clarify?

### OffsetDateTime

A Set is a collection that rejects duplicates according to equality rules.

It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Practical check:

- Define `OffsetDateTime` in one sentence.
- Recognize `OffsetDateTime` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `OffsetDateTime`.

Tiny example or mental model:

- When reading code, ask: what does `OffsetDateTime` change, allow, reject, or clarify?

### Instant

Instant represents a point on the UTC timeline.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Instant` in one sentence.
- Recognize `Instant` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Instant`.

Tiny example or mental model:

- When reading code, ask: what does `Instant` change, allow, reject, or clarify?

### Duration

Duration is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Duration` in one sentence.
- Recognize `Duration` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Duration`.

Tiny example or mental model:

- When reading code, ask: what does `Duration` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
