# Date and Time API - Part 2

## Learning Goal

This file covers a focused slice of **Date and Time API**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Period` |Period is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `DateTimeFormatter` |DateTimeFormatter is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `ZoneId` |ZoneId is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Parse date/time` |Parse date/time is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Format date/time` |Format date/time is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Compare date/time` |Compare date/time is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Add/subtract date/time` |Add/subtract date/time is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Timezone` |Timezone is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### Period

Period is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Period` in one sentence.
- Recognize `Period` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Period`.

Tiny example or mental model:

- When reading code, ask: what does `Period` change, allow, reject, or clarify?

### DateTimeFormatter

DateTimeFormatter is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `DateTimeFormatter` in one sentence.
- Recognize `DateTimeFormatter` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `DateTimeFormatter`.

Tiny example or mental model:

- When reading code, ask: what does `DateTimeFormatter` change, allow, reject, or clarify?

### ZoneId

ZoneId is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `ZoneId` in one sentence.
- Recognize `ZoneId` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `ZoneId`.

Tiny example or mental model:

- When reading code, ask: what does `ZoneId` change, allow, reject, or clarify?

### Parse date/time

Parse date/time is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Parse date/time` in one sentence.
- Recognize `Parse date/time` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Parse date/time`.

Tiny example or mental model:

- When reading code, ask: what does `Parse date/time` change, allow, reject, or clarify?

### Format date/time

Format date/time is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Format date/time` in one sentence.
- Recognize `Format date/time` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Format date/time`.

Tiny example or mental model:

- When reading code, ask: what does `Format date/time` change, allow, reject, or clarify?

### Compare date/time

Compare date/time is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Compare date/time` in one sentence.
- Recognize `Compare date/time` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Compare date/time`.

Tiny example or mental model:

- When reading code, ask: what does `Compare date/time` change, allow, reject, or clarify?

### Add/subtract date/time

Add/subtract date/time is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Add/subtract date/time` in one sentence.
- Recognize `Add/subtract date/time` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Add/subtract date/time`.

Tiny example or mental model:

- When reading code, ask: what does `Add/subtract date/time` change, allow, reject, or clarify?

### Timezone

Timezone is a specific concept in Date and Time API; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Timezone` in one sentence.
- Recognize `Timezone` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Timezone`.

Tiny example or mental model:

- When reading code, ask: what does `Timezone` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
