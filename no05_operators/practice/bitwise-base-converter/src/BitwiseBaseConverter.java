package no05_operators.practice.bitwise_base_converter;

/**
 * A utility class to convert integers to binary and hexadecimal strings
 * using raw bitwise operations (shifts and masks).
 * 
 * Note: Do NOT use Integer.toBinaryString() or Integer.toHexString() in your implementation!
 */
public class BitwiseBaseConverter {

    /**
     * Converts a 32-bit integer to its binary string representation.
     * The resulting string should omit leading zeros, except when the value is 0 (returns "0").
     *
     * @param value the integer value to convert
     * @return the binary string representation
     */
    public static String toBinary(int value) {
        // TODO: Implement binary conversion using bit shifting (>>>) and bitwise masking (&).
        return null;
    }

    /**
     * Converts a 32-bit integer to its hexadecimal string representation (lowercase).
     * The resulting string should omit leading zeros, except when the value is 0 (returns "0").
     *
     * @param value the integer value to convert
     * @return the hexadecimal string representation
     */
    public static String toHex(int value) {
        // TODO: Implement hexadecimal conversion using bit shifting (>>>) and bitwise masking (&).
        return null;
    }
}
