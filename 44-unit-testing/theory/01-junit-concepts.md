# Basic Unit Testing - Part 1

## Learning Goal

This file covers a focused slice of **Basic Unit Testing**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `JUnit` | JUnit is a Java testing framework for writing and running automated tests. |
| `Test case` |Test case is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Assertion` |Assertion is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `@Test` |@Test is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `@BeforeEach` |@BeforeEach is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `@AfterEach` |@AfterEach is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `@BeforeAll` |@BeforeAll is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `@AfterAll` |@AfterAll is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Basic Mockito` | Mockito is a mocking framework used to isolate units under test. |
| `Mock object` |Mock object is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### JUnit

JUnit is a Java testing framework for writing and running automated tests.

It matters because tests protect behavior during change. A common confusion is testing implementation details instead of observable behavior.

Practical check:

- Define `JUnit` in one sentence.
- Recognize `JUnit` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `JUnit`.

Tiny example or mental model:

- When reading code, ask: what does `JUnit` change, allow, reject, or clarify?

### Test case

Test case is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name.

It matters because tests protect behavior during change. A common confusion is testing implementation details instead of observable behavior.

Practical check:

- Define `Test case` in one sentence.
- Recognize `Test case` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Test case`.

Tiny example or mental model:

- When reading code, ask: what does `Test case` change, allow, reject, or clarify?

### Assertion

Assertion is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name.

It matters because tests protect behavior during change. A common confusion is testing implementation details instead of observable behavior.

Practical check:

- Define `Assertion` in one sentence.
- Recognize `Assertion` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Assertion`.

Tiny example or mental model:

- When reading code, ask: what does `Assertion` change, allow, reject, or clarify?

### @Test

@Test is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name.

It matters because tests protect behavior during change. A common confusion is testing implementation details instead of observable behavior.

Practical check:

- Define `@Test` in one sentence.
- Recognize `@Test` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `@Test`.

Tiny example or mental model:

- When reading code, ask: what does `@Test` change, allow, reject, or clarify?

### @BeforeEach

@BeforeEach is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `@BeforeEach` in one sentence.
- Recognize `@BeforeEach` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `@BeforeEach`.

Tiny example or mental model:

- When reading code, ask: what does `@BeforeEach` change, allow, reject, or clarify?

### @AfterEach

@AfterEach is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `@AfterEach` in one sentence.
- Recognize `@AfterEach` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `@AfterEach`.

Tiny example or mental model:

- When reading code, ask: what does `@AfterEach` change, allow, reject, or clarify?

### @BeforeAll

@BeforeAll is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `@BeforeAll` in one sentence.
- Recognize `@BeforeAll` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `@BeforeAll`.

Tiny example or mental model:

- When reading code, ask: what does `@BeforeAll` change, allow, reject, or clarify?

### @AfterAll

@AfterAll is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `@AfterAll` in one sentence.
- Recognize `@AfterAll` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `@AfterAll`.

Tiny example or mental model:

- When reading code, ask: what does `@AfterAll` change, allow, reject, or clarify?

### Basic Mockito

Mockito is a mocking framework used to isolate units under test.

It matters because tests protect behavior during change. A common confusion is testing implementation details instead of observable behavior.

Practical check:

- Define `Basic Mockito` in one sentence.
- Recognize `Basic Mockito` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Basic Mockito`.

Tiny example or mental model:

- When reading code, ask: what does `Basic Mockito` change, allow, reject, or clarify?

### Mock object

Mock object is a specific concept in Basic Unit Testing; learn its Java rule, valid use cases, and failure mode rather than only its name.

It matters because tests protect behavior during change. A common confusion is testing implementation details instead of observable behavior.

Practical check:

- Define `Mock object` in one sentence.
- Recognize `Mock object` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Mock object`.

Tiny example or mental model:

- When reading code, ask: what does `Mock object` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
