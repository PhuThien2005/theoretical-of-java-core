# Các Thuật Ngữ Của NIO / NIO.2

Hãy sử dụng tài liệu này khi một từ trong phần lý thuyết có vẻ quá cô đọng. Mỗi ngữ nghĩa đều có phần ý nghĩa, tầm quan trọng, hiểu lầm phổ biến và một ví dụ nhỏ.

## Đường dẫn (Path)

**`Đường dẫn (Path)`** — Đường dẫn (Path): Cung cấp các quy tắc và cơ chế hoạt động cụ thể trong lập trình Java.

## Files

**`Files`** — Lớp tiện ích NIO cung cấp các phương thức static để thao tác với tệp và thư mục.

## Kênh (Channel)

**`Kênh (Channel)`** — Kênh (Channel): Cung cấp các quy tắc và cơ chế hoạt động cụ thể trong lập trình Java.

## Bộ đệm (Buffer)

**`Bộ đệm (Buffer)`** — Bộ đệm (Buffer): Cung cấp các quy tắc và cơ chế hoạt động cụ thể trong lập trình Java.

## Bộ chọn (Selector)

**`Bộ chọn (Selector)`** — Bộ chọn (Selector): Cung cấp các quy tắc và cơ chế hoạt động cụ thể trong lập trình Java.

## IO bất đồng bộ (asynchronous IO)

**`IO bất đồng bộ (asynchronous IO)`** — IO bất đồng bộ (asynchronous IO): Cung cấp các quy tắc và cơ chế hoạt động cụ thể trong lập trình Java.

