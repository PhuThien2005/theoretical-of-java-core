# Thuật Ngữ API Date và Time

Dùng file này khi một từ trong lý thuyết cảm thấy quá cô đọng. Mỗi thuật ngữ có phần ý nghĩa, tầm quan trọng, điểm dễ nhầm lẫn và ví dụ nhỏ.

## LocalDate

`LocalDate` biểu diễn một ngày không có thời gian hay múi giờ.

Tại sao quan trọng: Dùng nó để dự đoán quy tắc Java chính xác, dạng hợp lệ và chế độ thất bại. Ôn với ví dụ nhỏ thay vì chỉ ghi nhớ nhãn.

Điểm dễ nhầm: Học viên thường chỉ thuộc `LocalDate` như một từ mà không giải thích được nó giải quyết vấn đề gì hay thay đổi quy tắc gì.

Ví dụ nhỏ: Khi đọc code, hỏi: `LocalDate` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## LocalTime

`LocalTime` là khái niệm cụ thể trong API Date và Time; hãy học quy tắc Java, trường hợp dùng hợp lệ và chế độ thất bại thay vì chỉ nhớ tên.

Tại sao quan trọng: Dùng nó để dự đoán quy tắc Java chính xác, dạng hợp lệ và chế độ thất bại. Ôn với ví dụ nhỏ thay vì chỉ ghi nhớ nhãn.

Điểm dễ nhầm: Học viên thường chỉ thuộc `LocalTime` như một từ mà không giải thích được nó giải quyết vấn đề gì hay thay đổi quy tắc gì.

Ví dụ nhỏ: Khi đọc code, hỏi: `LocalTime` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## Instant

`Instant` biểu diễn một thời điểm trên dòng thời gian UTC.

Tại sao quan trọng: Dùng nó để dự đoán quy tắc Java chính xác, dạng hợp lệ và chế độ thất bại. Ôn với ví dụ nhỏ thay vì chỉ ghi nhớ nhãn.

Điểm dễ nhầm: Học viên thường chỉ thuộc `Instant` như một từ mà không giải thích được nó giải quyết vấn đề gì hay thay đổi quy tắc gì.

Ví dụ nhỏ: Khi đọc code, hỏi: `Instant` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## Duration

`Duration` là khái niệm cụ thể trong API Date và Time; hãy học quy tắc Java, trường hợp dùng hợp lệ và chế độ thất bại thay vì chỉ nhớ tên.

Tại sao quan trọng: Dùng nó để dự đoán quy tắc Java chính xác, dạng hợp lệ và chế độ thất bại. Ôn với ví dụ nhỏ thay vì chỉ ghi nhớ nhãn.

Điểm dễ nhầm: Học viên thường chỉ thuộc `Duration` như một từ mà không giải thích được nó giải quyết vấn đề gì hay thay đổi quy tắc gì.

Ví dụ nhỏ: Khi đọc code, hỏi: `Duration` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## Period

`Period` là khái niệm cụ thể trong API Date và Time; hãy học quy tắc Java, trường hợp dùng hợp lệ và chế độ thất bại thay vì chỉ nhớ tên.

Tại sao quan trọng: Dùng nó để dự đoán quy tắc Java chính xác, dạng hợp lệ và chế độ thất bại. Ôn với ví dụ nhỏ thay vì chỉ ghi nhớ nhãn.

Điểm dễ nhầm: Học viên thường chỉ thuộc `Period` như một từ mà không giải thích được nó giải quyết vấn đề gì hay thay đổi quy tắc gì.

Ví dụ nhỏ: Khi đọc code, hỏi: `Period` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## ZoneId

`ZoneId` là khái niệm cụ thể trong API Date và Time; hãy học quy tắc Java, trường hợp dùng hợp lệ và chế độ thất bại thay vì chỉ nhớ tên.

Tại sao quan trọng: Dùng nó để dự đoán quy tắc Java chính xác, dạng hợp lệ và chế độ thất bại. Ôn với ví dụ nhỏ thay vì chỉ ghi nhớ nhãn.

Điểm dễ nhầm: Học viên thường chỉ thuộc `ZoneId` như một từ mà không giải thích được nó giải quyết vấn đề gì hay thay đổi quy tắc gì.

Ví dụ nhỏ: Khi đọc code, hỏi: `ZoneId` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## DateTimeFormatter

`DateTimeFormatter` là khái niệm cụ thể trong API Date và Time; hãy học quy tắc Java, trường hợp dùng hợp lệ và chế độ thất bại thay vì chỉ nhớ tên.

Tại sao quan trọng: Dùng nó để dự đoán quy tắc Java chính xác, dạng hợp lệ và chế độ thất bại. Ôn với ví dụ nhỏ thay vì chỉ ghi nhớ nhãn.

Điểm dễ nhầm: Học viên thường chỉ thuộc `DateTimeFormatter` như một từ mà không giải thích được nó giải quyết vấn đề gì hay thay đổi quy tắc gì.

Ví dụ nhỏ: Khi đọc code, hỏi: `DateTimeFormatter` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?
