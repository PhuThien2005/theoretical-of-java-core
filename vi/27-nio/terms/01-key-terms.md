# Thuật ngữ NIO / NIO.2 (NIO / NIO.2 Terms)

Sử dụng file này khi một từ ngữ trong phần lý thuyết có cảm giác quá ngắn gọn. Mỗi thuật ngữ đều có ý nghĩa, tầm quan trọng, điểm dễ gây nhầm lẫn và một ví dụ nhỏ.

## Path

`Path` là một khái niệm cụ thể trong NIO / NIO.2; hãy tìm hiểu quy tắc Java, các trường hợp sử dụng hợp lệ và chế độ lỗi của nó thay vì chỉ nhớ tên.

**Tại sao nó quan trọng:** Sử dụng nó để dự đoán chính xác quy tắc Java, biểu mẫu được phép và chế độ lỗi (failure mode). Xem xét nó với một ví dụ nhỏ thay vì chỉ ghi nhớ nhãn tên.

**Nhầm lẫn phổ biến:** Người học thường ghi nhớ `Path` như một từ đơn thuần nhưng không thể giải thích nó giải quyết vấn đề gì hoặc quy tắc nào nó thay đổi.

**Ví dụ nhỏ:** Khi đọc mã nguồn (code), hãy hỏi: `Path` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## Files

`Files` là một khái niệm cụ thể trong NIO / NIO.2; hãy tìm hiểu quy tắc Java, các trường hợp sử dụng hợp lệ và chế độ lỗi của nó thay vì chỉ nhớ tên.

**Tại sao nó quan trọng:** Sử dụng nó để dự đoán chính xác quy tắc Java, biểu mẫu được phép và chế độ lỗi (failure mode). Xem xét nó với một ví dụ nhỏ thay vì chỉ ghi nhớ nhãn tên.

**Nhầm lẫn phổ biến:** Người học thường ghi nhớ `Files` như một từ đơn thuần nhưng không thể giải thích nó giải quyết vấn đề gì hoặc quy tắc nào nó thay đổi.

**Ví dụ nhỏ:** Khi đọc mã nguồn (code), hãy hỏi: `Files` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## Channel

`Channel` là một khái niệm cụ thể trong NIO / NIO.2; hãy tìm hiểu quy tắc Java, các trường hợp sử dụng hợp lệ và chế độ lỗi của nó thay vì chỉ nhớ tên.

**Tại sao nó quan trọng:** Sử dụng nó để dự đoán chính xác quy tắc Java, biểu mẫu được phép và chế độ lỗi (failure mode). Xem xét nó với một ví dụ nhỏ thay vì chỉ ghi nhớ nhãn tên.

**Nhầm lẫn phổ biến:** Người học thường ghi nhớ `Channel` như một từ đơn thuần nhưng không thể giải thích nó giải quyết vấn đề gì hoặc quy tắc nào nó thay đổi.

**Ví dụ nhỏ:** Khi đọc mã nguồn (code), hãy hỏi: `Channel` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## Buffer

`Buffer` là một khái niệm cụ thể trong NIO / NIO.2; hãy tìm hiểu quy tắc Java, các trường hợp sử dụng hợp lệ và chế độ lỗi của nó thay vì chỉ nhớ tên.

**Tại sao nó quan trọng:** Sử dụng nó để dự đoán chính xác quy tắc Java, biểu mẫu được phép và chế độ lỗi (failure mode). Xem xét nó với một ví dụ nhỏ thay vì chỉ ghi nhớ nhãn tên.

**Nhầm lẫn phổ biến:** Người học thường ghi nhớ `Buffer` như một từ đơn thuần nhưng không thể giải thích nó giải quyết vấn đề gì hoặc quy tắc nào nó thay đổi.

**Ví dụ nhỏ:** Khi đọc mã nguồn (code), hãy hỏi: `Buffer` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## Selector

`Selector` là một khái niệm cụ thể trong NIO / NIO.2; hãy tìm hiểu quy tắc Java, các trường hợp sử dụng hợp lệ và chế độ lỗi của nó thay vì chỉ nhớ tên.

**Tại sao nó quan trọng:** Sử dụng nó để dự đoán chính xác quy tắc Java, biểu mẫu được phép và chế độ lỗi (failure mode). Xem xét nó với một ví dụ nhỏ thay vì chỉ ghi nhớ nhãn tên.

**Nhầm lẫn phổ biến:** Người học thường ghi nhớ `Selector` như một từ đơn thuần nhưng không thể giải thích nó giải quyết vấn đề gì hoặc quy tắc nào nó thay đổi.

**Ví dụ nhỏ:** Khi đọc mã nguồn (code), hãy hỏi: `Selector` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?

## asynchronous IO

`asynchronous IO` là một khái niệm cụ thể trong NIO / NIO.2; hãy tìm hiểu quy tắc Java, các trường hợp sử dụng hợp lệ và chế độ lỗi của nó thay vì chỉ nhớ tên.

**Tại sao nó quan trọng:** Sử dụng nó để dự đoán chính xác quy tắc Java, biểu mẫu được phép và chế độ lỗi (failure mode). Xem xét nó với một ví dụ nhỏ thay vì chỉ ghi nhớ nhãn tên.

**Nhầm lẫn phổ biến:** Người học thường ghi nhớ `asynchronous IO` như một từ đơn thuần nhưng không thể giải thích nó giải quyết vấn đề gì hoặc quy tắc nào nó thay đổi.

**Ví dụ nhỏ:** Khi đọc mã nguồn (code), hãy hỏi: `asynchronous IO` thay đổi, cho phép, từ chối hoặc làm rõ điều gì?
