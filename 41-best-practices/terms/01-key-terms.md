# Best Practices in Java Terms

Use this file when a word in the theory feels too compressed. Each term has meaning, importance, confusion, and a small example.

## composition

composition is a specific concept in Best Practices in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `composition` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `composition` change, allow, reject, or clarify?

## immutability

immutability is a specific concept in Best Practices in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `immutability` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `immutability` change, allow, reject, or clarify?

## raw type

raw type is a specific concept in Best Practices in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `raw type` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `raw type` change, allow, reject, or clarify?

## try-with-resources

Try-with-resources automatically closes resources that implement AutoCloseable.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `try-with-resources` as a word but cannot explain what problem it solves or what rule it changes.

Small example: `try { ... } catch (IOException ex) { ... }` handles a specific failure path.

## testable code

testable code is a specific concept in Best Practices in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: It matters because tests protect behavior during change. A common confusion is testing implementation details instead of observable behavior.

Common confusion: learners often memorize `testable code` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `testable code` change, allow, reject, or clarify?

## responsibility

responsibility is a specific concept in Best Practices in Java; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `responsibility` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `responsibility` change, allow, reject, or clarify?
