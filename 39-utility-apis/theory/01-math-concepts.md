# Some Common Utility APIs - Part 1

## Learning Goal

This file covers a focused slice of **Some Common Utility APIs**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Math` |Math is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Random` |Random is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `BigInteger` |BigInteger is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `BigDecimal` | BigDecimal represents decimal numbers precisely and is commonly used for money. |
| `UUID` |UUID is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Objects` |Objects is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Optional` | Optional is a container that may or may not hold a non-null value. |
| `System` |System is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Runtime` |Runtime is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `ProcessBuilder` |ProcessBuilder is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### Math

Math is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Math` in one sentence.
- Recognize `Math` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Math`.

Tiny example or mental model:

- When reading code, ask: what does `Math` change, allow, reject, or clarify?

### Random

Random is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Random` in one sentence.
- Recognize `Random` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Random`.

Tiny example or mental model:

- When reading code, ask: what does `Random` change, allow, reject, or clarify?

### BigInteger

BigInteger is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `BigInteger` in one sentence.
- Recognize `BigInteger` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `BigInteger`.

Tiny example or mental model:

- When reading code, ask: what does `BigInteger` change, allow, reject, or clarify?

### BigDecimal

BigDecimal represents decimal numbers precisely and is commonly used for money.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `BigDecimal` in one sentence.
- Recognize `BigDecimal` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `BigDecimal`.

Tiny example or mental model:

- When reading code, ask: what does `BigDecimal` change, allow, reject, or clarify?

### UUID

UUID is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `UUID` in one sentence.
- Recognize `UUID` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `UUID`.

Tiny example or mental model:

- When reading code, ask: what does `UUID` change, allow, reject, or clarify?

### Objects

Objects is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Objects` in one sentence.
- Recognize `Objects` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Objects`.

Tiny example or mental model:

- When reading code, ask: what does `Objects` change, allow, reject, or clarify?

### Optional

Optional is a container that may or may not hold a non-null value.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Optional` in one sentence.
- Recognize `Optional` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Optional`.

Tiny example or mental model:

- `Optional.ofNullable(value)` handles a possibly-null value.

### System

System is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `System` in one sentence.
- Recognize `System` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `System`.

Tiny example or mental model:

- When reading code, ask: what does `System` change, allow, reject, or clarify?

### Runtime

Runtime is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Runtime` in one sentence.
- Recognize `Runtime` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Runtime`.

Tiny example or mental model:

- When reading code, ask: what does `Runtime` change, allow, reject, or clarify?

### ProcessBuilder

ProcessBuilder is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `ProcessBuilder` in one sentence.
- Recognize `ProcessBuilder` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `ProcessBuilder`.

Tiny example or mental model:

- When reading code, ask: what does `ProcessBuilder` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
