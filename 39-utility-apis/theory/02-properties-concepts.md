# Some Common Utility APIs - Part 2

## Learning Goal

This file covers a focused slice of **Some Common Utility APIs**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Properties` |Properties is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `ResourceBundle` |ResourceBundle is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Locale` |Locale is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Currency` |Currency is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Formatter` |Formatter is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Scanner` |Scanner is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### Properties

Properties is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Properties` in one sentence.
- Recognize `Properties` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Properties`.

Tiny example or mental model:

- When reading code, ask: what does `Properties` change, allow, reject, or clarify?

### ResourceBundle

ResourceBundle is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `ResourceBundle` in one sentence.
- Recognize `ResourceBundle` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `ResourceBundle`.

Tiny example or mental model:

- When reading code, ask: what does `ResourceBundle` change, allow, reject, or clarify?

### Locale

Locale is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Locale` in one sentence.
- Recognize `Locale` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Locale`.

Tiny example or mental model:

- When reading code, ask: what does `Locale` change, allow, reject, or clarify?

### Currency

Currency is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Currency` in one sentence.
- Recognize `Currency` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Currency`.

Tiny example or mental model:

- When reading code, ask: what does `Currency` change, allow, reject, or clarify?

### Formatter

Formatter is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Formatter` in one sentence.
- Recognize `Formatter` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Formatter`.

Tiny example or mental model:

- When reading code, ask: what does `Formatter` change, allow, reject, or clarify?

### Scanner

Scanner is a specific concept in Some Common Utility APIs; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Scanner` in one sentence.
- Recognize `Scanner` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Scanner`.

Tiny example or mental model:

- When reading code, ask: what does `Scanner` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
