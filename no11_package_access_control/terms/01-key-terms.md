# Package and Access Control Terms

Use this file when a word in the theory feels too compressed. Each term has meaning, importance, confusion, and a small example.

## package

A package groups related classes and gives them a namespace.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `package` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `package` change, allow, reject, or clarify?

## import

import is a specific concept in Package and Access Control; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `import` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `import` change, allow, reject, or clarify?

## static import

Static means the member belongs to the class rather than to one particular object.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `static import` as a word but cannot explain what problem it solves or what rule it changes.

Small example: `ClassName.member` accesses a class-level member.

## default package

A package groups related classes and gives them a namespace.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `default package` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `default package` change, allow, reject, or clarify?

## classpath

Classpath tells the JVM and compiler where to find classes and JARs.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `classpath` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `classpath` change, allow, reject, or clarify?

## module path

Module path is the module-system-aware alternative to classpath for named modules.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `module path` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `module path` change, allow, reject, or clarify?
