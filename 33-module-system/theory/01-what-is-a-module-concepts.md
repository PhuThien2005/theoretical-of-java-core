# Java Module System - Part 1

## Learning Goal

This file covers a focused slice of **Java Module System**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `What is a module?` | What is a module is a key question for understanding Java Module System. |
| `module-info.java` |module-info.java is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `requires` |requires is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `exports` |exports is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `opens` |opens is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Named module` |Named module is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Unnamed module` |Unnamed module is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Automatic module` |Automatic module is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Module-level encapsulation` |Module-level encapsulation is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Module path vs Classpath` | Classpath tells the JVM and compiler where to find classes and JARs. |

## Detailed Notes

### What is a module?

What is a module is a key question for understanding Java Module System.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `What is a module?` in one sentence.
- Recognize `What is a module?` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `What is a module?`.

Tiny example or mental model:

- When reading code, ask: what does `What is a module?` change, allow, reject, or clarify?

### module-info.java

module-info.java is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `module-info.java` in one sentence.
- Recognize `module-info.java` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `module-info.java`.

Tiny example or mental model:

- When reading code, ask: what does `module-info.java` change, allow, reject, or clarify?

### requires

requires is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `requires` in one sentence.
- Recognize `requires` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `requires`.

Tiny example or mental model:

- When reading code, ask: what does `requires` change, allow, reject, or clarify?

### exports

exports is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `exports` in one sentence.
- Recognize `exports` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `exports`.

Tiny example or mental model:

- When reading code, ask: what does `exports` change, allow, reject, or clarify?

### opens

opens is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `opens` in one sentence.
- Recognize `opens` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `opens`.

Tiny example or mental model:

- When reading code, ask: what does `opens` change, allow, reject, or clarify?

### Named module

Named module is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Named module` in one sentence.
- Recognize `Named module` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Named module`.

Tiny example or mental model:

- When reading code, ask: what does `Named module` change, allow, reject, or clarify?

### Unnamed module

Unnamed module is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Unnamed module` in one sentence.
- Recognize `Unnamed module` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Unnamed module`.

Tiny example or mental model:

- When reading code, ask: what does `Unnamed module` change, allow, reject, or clarify?

### Automatic module

Automatic module is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Automatic module` in one sentence.
- Recognize `Automatic module` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Automatic module`.

Tiny example or mental model:

- When reading code, ask: what does `Automatic module` change, allow, reject, or clarify?

### Module-level encapsulation

Module-level encapsulation is a specific concept in Java Module System; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Module-level encapsulation` in one sentence.
- Recognize `Module-level encapsulation` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Module-level encapsulation`.

Tiny example or mental model:

- When reading code, ask: what does `Module-level encapsulation` change, allow, reject, or clarify?

### Module path vs Classpath

Classpath tells the JVM and compiler where to find classes and JARs.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Module path vs Classpath` in one sentence.
- Recognize `Module path vs Classpath` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Module path vs Classpath`.

Tiny example or mental model:

- When reading code, ask: what does `Module path vs Classpath` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
