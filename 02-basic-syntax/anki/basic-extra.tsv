Front	Back	Extra	Tags
Why must a public class name match the file name?	The Java compiler requires a public top-level class to be declared in a file with the same name.	`public class HelloWorld` should be in `HelloWorld.java`.	java::core::syntax java::interview
Why does `main` need to be static?	The JVM must call `main` without creating an object first.	`static` makes the method belong to the class rather than an instance.	java::core::syntax java::interview
What is the difference between `String[] args` and `String... args` in `main`?	Both can represent command-line arguments for `main`; `String... args` is varargs syntax.	`public static void main(String... args)` is accepted by Java.	java::core::syntax
What makes a comment useful?	A useful comment explains why code exists or clarifies a non-obvious decision.	Bad comments only repeat what the code already says.	java::core::syntax
Does `import java.util.*` import subpackages?	No. It imports types directly inside `java.util`, not types in subpackages.	For example, it does not import `java.util.concurrent.*`.	java::core::syntax
Why are naming conventions important?	They make code easier to read and help developers recognize classes, methods, variables, constants, and packages quickly.	Conventions are not enforced in every case, but ignoring them makes code harder to maintain.	java::core::syntax
Why can a variable declared inside an `if` block not be used outside it?	The variable's scope is limited to the block where it is declared.	This prevents accidental use of names outside their intended lifetime.	java::core::syntax
