# Exception Handling - Part 4

## Learning Goal

This file covers a focused slice of **Exception Handling**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `FileNotFoundException` | An exception represents an abnormal condition that a program may catch or propagate. |
| `SQLException` | An exception represents an abnormal condition that a program may catch or propagate. |
| `Best practices when handling exceptions` | An exception represents an abnormal condition that a program may catch or propagate. |

## Detailed Notes

### FileNotFoundException

An exception represents an abnormal condition that a program may catch or propagate.

It matters because exception behavior decides whether failures are handled locally, propagated, or allowed to stop the program. A common confusion is treating every exception the same instead of separating recoverable conditions from programming bugs.

Practical check:

- Define `FileNotFoundException` in one sentence.
- Recognize `FileNotFoundException` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `FileNotFoundException`.

Tiny example or mental model:

- When reading code, ask: what does `FileNotFoundException` change, allow, reject, or clarify?

### SQLException

An exception represents an abnormal condition that a program may catch or propagate.

It matters because exception behavior decides whether failures are handled locally, propagated, or allowed to stop the program. A common confusion is treating every exception the same instead of separating recoverable conditions from programming bugs.

Practical check:

- Define `SQLException` in one sentence.
- Recognize `SQLException` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `SQLException`.

Tiny example or mental model:

- When reading code, ask: what does `SQLException` change, allow, reject, or clarify?

### Best practices when handling exceptions

An exception represents an abnormal condition that a program may catch or propagate.

It matters because exception behavior decides whether failures are handled locally, propagated, or allowed to stop the program. A common confusion is treating every exception the same instead of separating recoverable conditions from programming bugs.

Practical check:

- Define `Best practices when handling exceptions` in one sentence.
- Recognize `Best practices when handling exceptions` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Best practices when handling exceptions`.

Tiny example or mental model:

- When reading code, ask: what does `Best practices when handling exceptions` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
