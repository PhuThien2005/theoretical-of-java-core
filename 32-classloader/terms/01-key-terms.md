# ClassLoader Terms

Use this file when a word in the theory feels too compressed. Each term has meaning, importance, confusion, and a small example.

## class loading

class loading is a specific concept in ClassLoader; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `class loading` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `class loading` change, allow, reject, or clarify?

## Bootstrap ClassLoader

A ClassLoader loads class definitions into the JVM.

Why it matters: It matters because runtime behavior explains performance, memory errors, startup behavior, and many interview questions. A common confusion is mixing compile-time concepts with JVM runtime services.

Common confusion: learners often memorize `Bootstrap ClassLoader` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `Bootstrap ClassLoader` change, allow, reject, or clarify?

## Application ClassLoader

A ClassLoader loads class definitions into the JVM.

Why it matters: It matters because runtime behavior explains performance, memory errors, startup behavior, and many interview questions. A common confusion is mixing compile-time concepts with JVM runtime services.

Common confusion: learners often memorize `Application ClassLoader` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `Application ClassLoader` change, allow, reject, or clarify?

## parent delegation

parent delegation is a specific concept in ClassLoader; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `parent delegation` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `parent delegation` change, allow, reject, or clarify?

## Class.forName

Class.forName is a specific concept in ClassLoader; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `Class.forName` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `Class.forName` change, allow, reject, or clarify?
