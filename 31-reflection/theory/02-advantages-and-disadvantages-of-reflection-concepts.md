# Reflection - Part 2

## Learning Goal

This file covers a focused slice of **Reflection**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Advantages and disadvantages of reflection` | Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime. |
| `Reflection in frameworks such as Spring` | Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime. |

## Detailed Notes

### Advantages and disadvantages of reflection

Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Advantages and disadvantages of reflection` in one sentence.
- Recognize `Advantages and disadvantages of reflection` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Advantages and disadvantages of reflection`.

Tiny example or mental model:

- When reading code, ask: what does `Advantages and disadvantages of reflection` change, allow, reject, or clarify?

### Reflection in frameworks such as Spring

Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Reflection in frameworks such as Spring` in one sentence.
- Recognize `Reflection in frameworks such as Spring` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Reflection in frameworks such as Spring`.

Tiny example or mental model:

- When reading code, ask: what does `Reflection in frameworks such as Spring` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
