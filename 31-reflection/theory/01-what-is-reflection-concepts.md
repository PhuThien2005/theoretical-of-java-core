# Reflection - Part 1

## Learning Goal

This file covers a focused slice of **Reflection**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `What is Reflection?` | Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime. |
| `Class<?>` |Class<?> is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Get class information` |Get class information is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Get field` |Get field is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Get method` |Get method is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Get constructor` |Get constructor is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Invoke method using reflection` | Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime. |
| `Create object using reflection` | Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime. |
| `Access private field/method` |Access private field/method is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Annotation + reflection` | An annotation attaches metadata to program elements such as classes, methods, or fields. |

## Detailed Notes

### What is Reflection?

Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `What is Reflection?` in one sentence.
- Recognize `What is Reflection?` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `What is Reflection?`.

Tiny example or mental model:

- When reading code, ask: what does `What is Reflection?` change, allow, reject, or clarify?

### Class<?>

Class<?> is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Class<?>` in one sentence.
- Recognize `Class<?>` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Class<?>`.

Tiny example or mental model:

- When reading code, ask: what does `Class<?>` change, allow, reject, or clarify?

### Get class information

Get class information is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Get class information` in one sentence.
- Recognize `Get class information` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Get class information`.

Tiny example or mental model:

- When reading code, ask: what does `Get class information` change, allow, reject, or clarify?

### Get field

Get field is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Get field` in one sentence.
- Recognize `Get field` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Get field`.

Tiny example or mental model:

- When reading code, ask: what does `Get field` change, allow, reject, or clarify?

### Get method

Get method is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Get method` in one sentence.
- Recognize `Get method` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Get method`.

Tiny example or mental model:

- When reading code, ask: what does `Get method` change, allow, reject, or clarify?

### Get constructor

Get constructor is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Get constructor` in one sentence.
- Recognize `Get constructor` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Get constructor`.

Tiny example or mental model:

- When reading code, ask: what does `Get constructor` change, allow, reject, or clarify?

### Invoke method using reflection

Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Invoke method using reflection` in one sentence.
- Recognize `Invoke method using reflection` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Invoke method using reflection`.

Tiny example or mental model:

- When reading code, ask: what does `Invoke method using reflection` change, allow, reject, or clarify?

### Create object using reflection

Reflection lets code inspect and manipulate classes, fields, methods, and constructors at runtime.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Create object using reflection` in one sentence.
- Recognize `Create object using reflection` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Create object using reflection`.

Tiny example or mental model:

- When reading code, ask: what does `Create object using reflection` change, allow, reject, or clarify?

### Access private field/method

Access private field/method is a specific concept in Reflection; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Access private field/method` in one sentence.
- Recognize `Access private field/method` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Access private field/method`.

Tiny example or mental model:

- When reading code, ask: what does `Access private field/method` change, allow, reject, or clarify?

### Annotation + reflection

An annotation attaches metadata to program elements such as classes, methods, or fields.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Annotation + reflection` in one sentence.
- Recognize `Annotation + reflection` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Annotation + reflection`.

Tiny example or mental model:

- When reading code, ask: what does `Annotation + reflection` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
