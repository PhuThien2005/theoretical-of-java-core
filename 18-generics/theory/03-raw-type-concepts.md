# Generics - Part 3

## Learning Goal

This file covers a focused slice of **Generics**. Study each concept as a practical Java rule, not as isolated vocabulary.

## Outline Coverage

| Concept | What to know |
| --- | --- |
| `Raw type` |Raw type is a specific concept in Generics; learn its Java rule, valid use cases, and failure mode rather than only its name. |
| `Generic limitations` |Generic limitations is a specific concept in Generics; learn its Java rule, valid use cases, and failure mode rather than only its name. |

## Detailed Notes

### Raw type

Raw type is a specific concept in Generics; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Raw type` in one sentence.
- Recognize `Raw type` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Raw type`.

Tiny example or mental model:

- When reading code, ask: what does `Raw type` change, allow, reject, or clarify?

### Generic limitations

Generic limitations is a specific concept in Generics; learn its Java rule, valid use cases, and failure mode rather than only its name.

Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Practical check:

- Define `Generic limitations` in one sentence.
- Recognize `Generic limitations` in code, commands, documentation, or interview prompts.
- Explain one bug, limitation, or tradeoff related to `Generic limitations`.

Tiny example or mental model:

- When reading code, ask: what does `Generic limitations` change, allow, reject, or clarify?

## Common Review Prompts

- Which concepts here are compile-time rules?
- Which concepts here affect runtime behavior?
- Which concepts here are likely interview traps?
