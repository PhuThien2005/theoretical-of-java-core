# Optional Terms

Use this file when a word in the theory feels too compressed. Each term has meaning, importance, confusion, and a small example.

## Optional

Optional is a container that may or may not hold a non-null value.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `Optional` as a word but cannot explain what problem it solves or what rule it changes.

Small example: `Optional.ofNullable(value)` handles a possibly-null value.

## empty optional

Optional is a container that may or may not hold a non-null value.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `empty optional` as a word but cannot explain what problem it solves or what rule it changes.

Small example: `Optional.ofNullable(value)` handles a possibly-null value.

## fallback value

fallback value is a specific concept in Optional; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `fallback value` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `fallback value` change, allow, reject, or clarify?

## map

A Map stores key-value pairs and retrieves values by key.

Why it matters: It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Common confusion: learners often memorize `map` as a word but cannot explain what problem it solves or what rule it changes.

Small example: `Map<String, Integer> scores = new HashMap<>();` maps keys to values.

## flatMap

A Map stores key-value pairs and retrieves values by key.

Why it matters: It matters because choosing the wrong data structure changes correctness, performance, and duplicate-handling behavior. A common confusion is memorizing class names without knowing lookup order, equality rules, or iteration behavior.

Common confusion: learners often memorize `flatMap` as a word but cannot explain what problem it solves or what rule it changes.

Small example: `Map<String, Integer> scores = new HashMap<>();` maps keys to values.

## optional misuse

Optional is a container that may or may not hold a non-null value.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `optional misuse` as a word but cannot explain what problem it solves or what rule it changes.

Small example: `Optional.ofNullable(value)` handles a possibly-null value.
