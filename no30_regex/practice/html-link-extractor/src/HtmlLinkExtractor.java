package no30_regex.practice.html_link_extractor;

import java.util.List;
import java.util.regex.Pattern;

/**
 * Starter template for extracting links from HTML anchor tags using regex.
 */
public class HtmlLinkExtractor {

    // TODO: Define and compile the Pattern to extract anchor hrefs:
    // Format: <a href="URL">
    // Extract: URL
    private static final Pattern LINK_PATTERN = null;

    /**
     * Finds and returns a list of all URLs inside href attributes of anchor tags.
     * Return an empty list if no links are found or if HTML input is null/empty.
     */
    public static List<String> extractLinks(String html) {
        // TODO: Implement parsing and return matched links
        return null;
    }
}
