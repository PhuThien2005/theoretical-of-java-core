package my.module;

import java.util.logging.Logger;

/**
 * Starter template for a modular application class using logging.
 */
public class ModularApp {

    private static final Logger logger = Logger.getLogger(ModularApp.class.getName());

    public static void runLogMessage(String msg) {
        // TODO: Log the message at INFO level.
        // This requires 'java.logging' platform module to compile and run in a module environment!
    }
}
