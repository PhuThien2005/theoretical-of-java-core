package no01_overview.practice.runtime_inspector;

/**
 * Starter template for inspecting JVM runtime memory allocations.
 */
public class RuntimeInspector {

    /**
     * Returns the maximum amount of memory (in bytes) that the JVM will attempt to use.
     */
    public static long getMaxMemory() {
        // TODO: Retrieve max memory from runtime
        return 0;
    }

    /**
     * Returns the total amount of memory (in bytes) currently allocated to the JVM.
     */
    public static long getTotalMemory() {
        // TODO: Retrieve total memory from runtime
        return 0;
    }

    /**
     * Returns the amount of memory (in bytes) currently used by the JVM heap.
     * Formula: Total Memory - Free Memory
     */
    public static long getUsedMemory() {
        // TODO: Calculate used memory from total and free memory
        return 0;
    }
}
