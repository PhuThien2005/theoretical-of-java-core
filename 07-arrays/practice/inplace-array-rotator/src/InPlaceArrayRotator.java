/**
 * A utility class to rotate 1D arrays in-place.
 */
public class InPlaceArrayRotator {

    /**
     * Rotates the given array to the right by k steps in-place.
     * The space complexity must be O(1) (no extra arrays allocated).
     * 
     * If the array is null or empty, the method should return without modifying anything.
     * If k is negative, rotate to the left by |k| steps (which is equivalent to a right
     * rotation of arr.length - |k|).
     *
     * Example:
     * arr = [1, 2, 3, 4, 5], k = 2
     * Output: [4, 5, 1, 2, 3]
     *
     * @param arr the array to rotate in-place
     * @param k the number of steps to rotate the array
     */
    public static void rotate(int[] arr, int k) {
        // TODO: Implement in-place rotation using array reversals.
    }
}
