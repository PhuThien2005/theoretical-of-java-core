/**
 * A utility class to perform in-place operations on 2D matrices.
 */
public class MatrixTransposer {

    /**
     * Transposes an N x N square matrix in-place.
     * Transposition swaps elements across the diagonal: matrix[i][j] <-> matrix[j][i].
     * 
     * @param matrix the square matrix to transpose in-place
     * @throws IllegalArgumentException if the matrix is null, not square, or contains non-uniform rows
     */
    public static void transpose(int[][] matrix) {
        // TODO: Implement square matrix transposition in-place.
    }

    /**
     * Sorts each row of the 2D matrix in ascending order.
     * Uses standard sorting tools or algorithms.
     * 
     * @param matrix the matrix whose rows should be sorted
     * @throws IllegalArgumentException if the matrix is null
     */
    public static void sortRows(int[][] matrix) {
        // TODO: Implement row-by-row sorting.
    }
}
