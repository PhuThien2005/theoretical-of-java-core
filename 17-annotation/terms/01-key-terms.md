# Annotation Terms

Use this file when a word in the theory feels too compressed. Each term has meaning, importance, confusion, and a small example.

## annotation

An annotation attaches metadata to program elements such as classes, methods, or fields.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `annotation` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `annotation` change, allow, reject, or clarify?

## built-in annotation

An annotation attaches metadata to program elements such as classes, methods, or fields.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `built-in annotation` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `built-in annotation` change, allow, reject, or clarify?

## meta-annotation

An annotation attaches metadata to program elements such as classes, methods, or fields.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `meta-annotation` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `meta-annotation` change, allow, reject, or clarify?

## retention

retention is a specific concept in Annotation; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `retention` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `retention` change, allow, reject, or clarify?

## target

target is a specific concept in Annotation; learn its Java rule, valid use cases, and failure mode rather than only its name.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `target` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `target` change, allow, reject, or clarify?

## annotation processing

An annotation attaches metadata to program elements such as classes, methods, or fields.

Why it matters: Use it to predict the exact Java rule, the allowed form, and the failure mode. Review it with a tiny example instead of memorizing only the label.

Common confusion: learners often memorize `annotation processing` as a word but cannot explain what problem it solves or what rule it changes.

Small example: When reading code, ask: what does `annotation processing` change, allow, reject, or clarify?
